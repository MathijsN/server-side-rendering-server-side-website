{% render 'partials/head.liquid', title: 'Homepage' %}

<main>
{% render 'partials/top-navigation.liquid', title: snappmapsFiltered[0].name, allSnappmaps: allSnappmaps %}
  <h2>Group 2B</h2>
  <ul class="grid-view">
    {% for snappmap in snappmapsFiltered %}
      {% for snap in snappmap.snaps %}
        {% if snap.picture != null %}
          <li>
            <a href="/snappmaps/snapp/{{ snap.uuid }}">
                <img src="https://fdnd-agency.directus.app/assets/{{ snap.picture }}" loading="lazy">
            </a>
          </li>
        {% endif %}
      {% endfor %}
    {% endfor %}
  </ul>

  {% render 'partials/bottom-navigation.liquid' %}
</main>
